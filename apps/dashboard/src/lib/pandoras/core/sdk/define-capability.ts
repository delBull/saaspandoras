import { CapabilityDefinition } from '../capabilities/capability-runtime';

/**
 * Define de manera declarativa una Capacidad que el SO puede resolver.
 */
export function defineCapability(config: CapabilityDefinition): CapabilityDefinition {
  return config;
}
