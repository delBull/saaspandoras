import React from 'react';
import UnifiedOnboardingClient from './UnifiedOnboardingClient';

export const dynamic = 'force-dynamic';

export default function UnifiedOnboardingPage() {
  return <UnifiedOnboardingClient />;
}
