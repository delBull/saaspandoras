{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "turbo run build --filter=@saasfly/dashboard...",
  "installCommand": "bun install --frozen-lockfile",
  "crons": [
    {
      "path": "/api/cron/marketing",
      "schedule": "0 10 * * *"
    }
  ]
}