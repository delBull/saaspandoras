import postgres from "postgres"; import dotenv from "dotenv"; dotenv.config({path: "../../.env"}); const sql = postgres(process.env.DATABASE_URL); sql`SELECT column_name FROM information_schema.columns WHERE table_name = 'users'`.then(res => console.log(res.map(r=>r.column_name))).catch(console.error).finally(() => process.exit(0));
