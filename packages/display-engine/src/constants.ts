import { SovereignDisplayProfile } from "./types";

export const DEFAULT_DISPLAY_PROFILE: SovereignDisplayProfile = {
  scale: "100",
  theme: "base",
  magnifier: false,
  reducedMotion: false,
};

export const LOCAL_STORAGE_KEY = "pandoras:display-profile";
