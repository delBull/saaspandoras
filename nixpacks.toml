[providers]
  node = "20"

[phases.setup]
  nixPkgs = ["...", "python3", "gcc", "gnumake"]

[phases.install]
  cmds = ["bun install --frozen-lockfile"]

[phases.build]
  cmds = ["bun run build:railway"]
