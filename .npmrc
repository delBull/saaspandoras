# Usar el registry oficial primario de npm con reintentos para evitar bloqueos 429 en CI/CD (Railway/Vercel)
registry=https://registry.npmjs.org/
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
maxsockets=5
