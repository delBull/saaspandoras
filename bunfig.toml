# Configuración de Bun para Railway y CI/CD
[install]
# Configurar reintentos automáticos para evitar errores 429 Too Many Requests de npmjs.org
backend = "copyfile"

[install.cache]
dir = "~/.bun/install/cache"

[install.scopes]

[install.lockfile]
print = "warn"
